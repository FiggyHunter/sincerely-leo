---
import CategoryLayout from "@/layouts/CategoryLayout.astro";
import { getPhotographs } from "@/api/photographs/getPhotographs";
import { getPhotographUrl } from "@/api/photographs/getPhotographUrl";

import { Image } from "astro:assets";
import ImageFullScreen from "@/components/ImageFullScreen.vue";

const photos = await getPhotographs();

console.log(photos[0].attributes.metadata);
---

<CategoryLayout>
  <ImageFullScreen client:only />
</CategoryLayout>

<script>
  document.querySelectorAll(".image-gallery__image img")[0].loading = "auto";
</script>
